# Deprecated — use web/ for the app

This folder is the **old Expo / React Native** frontend. The project is **web-only** now.

- **Use `../web`** for the React (Vite) website. Run `npm run dev` there and open http://localhost:5173.
- Backend is still **api/** on port 8000.

This Expo app is kept for reference only and is not used for deployment.
